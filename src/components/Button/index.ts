import Button from '@mui/material/Button';
import { default as Mint } from './MintButton';
import { default as Minifiable } from './MinifiableButton';
import { default as Animate } from './AnimateButton';

export default {
  Mint,
  Minifiable,
  Animate,
  Standard: Button
}